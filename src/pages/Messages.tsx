import ChatLayout from './messages/ChatLayout';

const Messages = () => {
    return (
        <div className="h-full">
            <ChatLayout />
        </div>
    );
};

export default Messages;
